node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.env
.env.*
# Local CV upload fallback (dev only)
uploads/
# curl cookie jars from local API testing — contain session tokens
*cookies*.txt
